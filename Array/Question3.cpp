#include <iostream>
using namespace std;

int main()
{
    int n;
    cout << "Enter the length of the array\n";
    cin >> n;
    int array[n];

    for (int i = 0; i < n; i++)
    {
        
        cin >> array[i];
    }

    int k = 0, dif1 = array[1] - array[0], maxNo = 0;
    ;
    while (k < n)
    {
       
        k++;
    }
    return 0;
}

